#include <stdio.h>
	main ()
{
int nombre;
int pair;
int impair;
printf ("Saisir nombre\n");
scanf ("%d", &nombre);;
scanf ("%c", pair);
scanf ("%c", impair);
if nombre%2=0, printf (%c, pair)
else printf (%c, impair)
}
