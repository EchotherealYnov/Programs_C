#include <stdio.h>
#include <string.h>

 
int main()
{
    int tabvaleur[20] = {12,7,8,1,3,15,5,-1}, i = 0;

    for (i = 0 ; i < 11 ; i++)
    {
        printf("%d\t",tabvaleur[i]);
    }

    return 0;
}
